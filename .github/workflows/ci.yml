name: CI/CD

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          
      - name: Install dependencies
        run: npm ci
        
      - name: Lint and Type check
        run: |
          npm run lint
          npx tsc --noEmit
        
      - name: Run tests
        run: npm run test
        
      - name: Run E2E tests
        run: |
          npm run build
          npm run start & npx wait-on http://localhost:3000
          npm run test:e2e
          
  deploy:
    needs: [test]
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Deploy to Vercel
        uses: amondnet/vercel-action@v20
        with:
          vercel-token: ${{secrets.ZFWAZFF4KDeAwPfU0gt66xsy}}
          vercel-org-id: ${{secrets.team_MFxXZ7A8QT8xrL1AtirsBueo}}
          vercel-project-id: ${{secrets.prj_HNXFuoTYxr4ofgiQh5FpSqKfxO3Y}}
          vercel-args: '--prod'